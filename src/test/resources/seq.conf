service.hazelcast {
    properties {
        hazelcast.logging.type = slf4j
    }
}

// Predefined persistence config
persistence.zk{
    type = zookeeper
}

// Used for first create
sequence += {
    match = cx_*
    persistence = zk
    attrs {
        inc = 1
        min = 100
    }
}
